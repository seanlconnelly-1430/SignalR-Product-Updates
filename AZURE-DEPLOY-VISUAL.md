# Azure Deployment Visual Guide

## Deployment Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    START: Local Development                      │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 1: Run Deployment Script                                  │
│  $ ./deploy-azure.sh                                            │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 2: Azure Resource Creation                                │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ • Resource Group (signalr-rg)                           │   │
│  │ • Container Registry (signalracr)                       │   │
│  │ • Container Apps Environment (signalr-env)              │   │
│  └─────────────────────────────────────────────────────────┘   │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 3: Build Docker Images                                    │
│  ┌──────────────────────────┐  ┌──────────────────────────┐    │
│  │  Server Image            │  │  Client Image            │    │
│  │  • .NET 10.0 SDK         │  │  • Node 23-alpine        │    │
│  │  • Multi-stage build     │  │  • Angular build         │    │
│  │  • Health check          │  │  • Nginx serve           │    │
│  │  • Port 5170             │  │  • Port 80               │    │
│  └──────────────────────────┘  └──────────────────────────┘    │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 4: Push to Azure Container Registry                       │
│  signalracr.azurecr.io/signalr-server:latest                   │
│  signalracr.azurecr.io/signalr-client:latest                   │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 5: Deploy Server Container App                            │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  signalr-server                                         │   │
│  │  • CPU: 0.5 cores                                       │   │
│  │  • Memory: 1 GiB                                        │   │
│  │  • Replicas: 1-3 (auto-scale)                          │   │
│  │  • URL: https://signalr-server.*.azurecontainerapps.io │   │
│  └─────────────────────────────────────────────────────────┘   │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 6: Update Client Configuration                            │
│  • Update environment.azure.ts with actual server URL           │
│  • Rebuild client image                                         │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 7: Deploy Client Container App                            │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  signalr-client                                         │   │
│  │  • CPU: 0.25 cores                                      │   │
│  │  • Memory: 0.5 GiB                                      │   │
│  │  • Replicas: 1-3 (auto-scale)                          │   │
│  │  • URL: https://signalr-client.*.azurecontainerapps.io │   │
│  └─────────────────────────────────────────────────────────┘   │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                    SUCCESS: Application Running                  │
│  Test at: https://signalr-client.*.azurecontainerapps.io       │
└─────────────────────────────────────────────────────────────────┘
```

## Architecture in Azure

```
                    ┌─────────────────────────────────────┐
                    │          Internet Users              │
                    └──────────────┬──────────────────────┘
                                   │ HTTPS
                    ┌──────────────┴──────────────────────┐
                    │                                      │
                    │                                      │
            ┌───────▼────────┐                  ┌─────────▼────────┐
            │  Azure Ingress │                  │   Azure Ingress  │
            │   (Client)     │                  │    (Server)      │
            └───────┬────────┘                  └─────────┬────────┘
                    │                                     │
            ┌───────▼────────────────────────────────────▼────────┐
            │     Container Apps Environment (signalr-env)        │
            │                                                      │
            │  ┌──────────────────┐      ┌──────────────────┐   │
            │  │  signalr-client  │      │  signalr-server  │   │
            │  │                  │      │                  │   │
            │  │  Angular App     │◄─────┤  ASP.NET Core   │   │
            │  │  (Nginx)         │HTTP  │  + SignalR Hub  │   │
            │  │                  │      │                  │   │
            │  │  Port: 80        │◄─────┤  Port: 5170     │   │
            │  │  Replicas: 1-3   │WebSkt│  Replicas: 1-3  │   │
            │  │                  │      │                  │   │
            │  └──────────────────┘      └──────────────────┘   │
            │                                                      │
            └──────────────────────────────────────────────────────┘
                                │
                                │ Pull Images
                                │
                    ┌───────────▼──────────────┐
                    │  Azure Container Registry │
                    │      (signalracr)        │
                    │                          │
                    │  • signalr-server:latest │
                    │  • signalr-client:latest │
                    └──────────────────────────┘
```

## File Structure After Azure Setup

```
SignalR/
│
├── Documentation
│   ├── README.md                    # Main project documentation
│   ├── DOCKER.md                    # Docker deployment guide
│   ├── AZURE.md                     # ⭐ Azure step-by-step guide
│   ├── AZURE-CONFIG.md              # ⭐ Azure configuration details
│   ├── AZURE-QUICKREF.md            # ⭐ Quick command reference
│   ├── AZURE-SUMMARY.md             # ⭐ Summary of all changes
│   └── AZURE-DEPLOY-VISUAL.md       # ⭐ This visual guide
│
├── Deployment Scripts
│   ├── deploy-azure.sh              # ⭐ Automated Azure deployment
│   └── docker-compose.yml           # Docker Compose (local dev)
│
├── Server
│   ├── ProductUpdatesServer/
│   │   ├── Controllers/
│   │   ├── Hubs/
│   │   ├── Models/
│   │   ├── Program.cs               # ✏️ Updated with /health endpoint
│   │   ├── appsettings.json         # ✏️ Updated CORS configuration
│   │   ├── appsettings.Production.json # ⭐ Azure production settings
│   │   ├── Dockerfile               # Docker Compose
│   │   └── Dockerfile.azure         # ⭐ Azure Container Apps
│
└── Client
    ├── product-updates-client/
    │   ├── src/
    │   │   ├── app/
    │   │   └── environments/
    │   │       ├── environment.ts           # Development (localhost)
    │   │       ├── environment.prod.ts      # ✏️ Production (Azure)
    │   │       ├── environment.azure.ts     # ⭐ Azure-specific
    │   │       └── environment.docker.ts    # ⭐ Docker Compose
    │   ├── angular.json             # ✏️ Added azure/docker configs
    │   ├── Dockerfile               # Docker Compose
    │   ├── Dockerfile.azure         # ⭐ Azure Container Apps
    │   ├── nginx.conf               # Docker Compose
    │   └── nginx.azure.conf         # ⭐ Azure production

Legend:
  ⭐ New file for Azure
  ✏️ Modified for Azure support
```

## Environment Configuration Matrix

```
┌──────────────┬──────────────────────┬──────────────────────┬────────────────┐
│ Environment  │ Server URL           │ Build Command        │ Use Case       │
├──────────────┼──────────────────────┼──────────────────────┼────────────────┤
│ Development  │ http://localhost     │ ng serve             │ Local dev      │
│              │ :5170                │                      │ with ng serve  │
├──────────────┼──────────────────────┼──────────────────────┼────────────────┤
│ Docker       │ http://server:5170   │ ng build             │ Docker Compose │
│              │                      │ --configuration      │ local testing  │
│              │                      │ =docker              │                │
├──────────────┼──────────────────────┼──────────────────────┼────────────────┤
│ Azure        │ https://signalr-     │ ng build             │ Azure Container│
│              │ server.*.azurecon    │ --configuration      │ Apps production│
│              │ tainerapps.io        │ =azure               │                │
├──────────────┼──────────────────────┼──────────────────────┼────────────────┤
│ Production   │ [Custom Domain]      │ ng build             │ Generic prod   │
│              │                      │ --configuration      │ deployment     │
│              │                      │ =production          │                │
└──────────────┴──────────────────────┴──────────────────────┴────────────────┘
```

## Health Check Flow

```
Azure Container Apps Health Monitor
          │
          ├──────────────────────────────────────┐
          │                                      │
          ▼                                      ▼
┌─────────────────────┐              ┌─────────────────────┐
│  Check Client       │              │  Check Server       │
│  Every 30s          │              │  Every 30s          │
│                     │              │                     │
│  GET /health        │              │  GET /health        │
│                     │              │                     │
│  Expected: 200 OK   │              │  Expected: 200 OK   │
│  "healthy"          │              │  {"status":         │
│                     │              │   "healthy"}        │
└─────────┬───────────┘              └─────────┬───────────┘
          │                                    │
          ├──────────┬─────────────────────────┤
          │          │                         │
          ▼          ▼                         ▼
      ┌────────┐  ┌────────┐             ┌────────┐
      │ Healthy│  │ Failed │             │ Restart│
      │ Route  │  │ 3 times│             │ Pod    │
      │ Traffic│  │        │             │        │
      └────────┘  └────────┘             └────────┘
```

## Scaling Behavior

```
Request Load Over Time

High Traffic ─┐                                    ┌─────────
              │              ┌───────┐             │
              │         ┌────┤       ├────┐        │
              │    ┌────┤    │       │    ├────┐   │
Medium ───────┤────┤    │    │       │    │    ├───┤
              │    │    │    │       │    │    │   │
Low ──────────┴────┴────┴────┴───────┴────┴────┴───┴─────────
              
              Time ───────────────────────────────────────►

Replica Count

3 replicas ───┐                                    ┌─────────
              │              ┌───────┐             │
              │         ┌────┤       ├────┐        │
              │    ┌────┤    │       │    ├────┐   │
2 replicas ───┤────┤    │    │       │    │    ├───┤
              │    │    │    │       │    │    │   │
1 replica ────┴────┴────┴────┴───────┴────┴────┴───┴─────────

              Time ───────────────────────────────────────►

Auto-scaling triggers:
• CPU > 70%: Scale up
• Memory > 80%: Scale up
• CPU < 30% for 5 min: Scale down
• Minimum: 1 replica
• Maximum: 3 replicas
```

## Cost Over Time

```
Monthly Cost Estimate

$30 ┤                                               
    ┤     ┌─────────────────────────┐              
$25 ┤     │  3 replicas             │              
    ┤     │  (peak load)            │              
$20 ┤─────┤                         ├──────────────
    ┤     │                         │              
$15 ┤     │  1-2 replicas           │              
    ┤─────┤  (normal load)          ├──────────────
$10 ┤     │                         │              
    ┤     │                         │              
$5  ┤─────┴─────────────────────────┴──────────────
    ┤     Registry + base resources                
$0  └─────┬─────────┬──────────┬────────┬─────────
        Week 1   Week 2    Week 3    Week 4

Breakdown:
• Container Registry (Basic): ~$5/month
• Server (0.5 vCPU, 1 GiB): ~$10-15/month
• Client (0.25 vCPU, 0.5 GiB): ~$5-8/month
────────────────────────────────────────────
Total: $20-28/month (with normal usage)

💡 Tip: Scale to 0 replicas when not in use to save costs!
```

## SignalR Connection Flow

```
Browser                 Client Container          Server Container
   │                          │                          │
   │ 1. Load Angular App      │                          │
   ├─────────────────────────►│                          │
   │                          │                          │
   │ 2. Serve index.html      │                          │
   │◄─────────────────────────┤                          │
   │                          │                          │
   │ 3. HTTP: Get Products    │                          │
   ├─────────────────────────►├─────────────────────────►│
   │                          │  GET /api/products       │
   │                          │                          │
   │ 4. Return Product List   │                          │
   │◄─────────────────────────┤◄─────────────────────────┤
   │                          │                          │
   │ 5. Negotiate SignalR     │                          │
   ├─────────────────────────►├─────────────────────────►│
   │                          │  POST /productHub/       │
   │                          │       negotiate          │
   │                          │                          │
   │ 6. Connection Token      │                          │
   │◄─────────────────────────┤◄─────────────────────────┤
   │                          │                          │
   │ 7. WebSocket Upgrade     │                          │
   ├─────────────────────────►├─────────────────────────►│
   │                          │  Upgrade: websocket      │
   │                          │                          │
   │ 8. WebSocket Connected   │                          │
   │◄═════════════════════════╪══════════════════════════╪
   │          Persistent WebSocket Connection            │
   │                          │                          │
   │ 9. User adds product     │                          │
   ├─────────────────────────►├─────────────────────────►│
   │                          │  POST /api/products      │
   │                          │                          │
   │                          │ 10. Broadcast to all     │
   │◄═════════════════════════╪══════════════════════════┤
   │    ProductAdded event    │                          │
   │    (via WebSocket)       │                          │
   │                          │                          │
   │ 11. UI updates           │                          │
   │     automatically        │                          │
   │                          │                          │
```

## Quick Start Checklist

```
□ Step 1: Prerequisites
   □ Azure CLI installed (az --version)
   □ Docker installed and running
   □ Azure subscription active
   □ Logged into Azure (az login)

□ Step 2: Review Configuration
   □ Check AZURE-SUMMARY.md
   □ Review estimated costs (~$20-28/month)
   □ Understand resource group naming (signalr-rg)

□ Step 3: Deploy
   □ Make deploy script executable (already done)
   □ Run: ./deploy-azure.sh
   □ Wait ~10-15 minutes for completion

□ Step 4: Test
   □ Open client URL in browser
   □ Check health endpoints
   □ Add a product
   □ Open another browser to verify real-time updates

□ Step 5: Monitor
   □ Check logs: az containerapp logs show ...
   □ Monitor costs in Azure Portal
   □ Set up budget alerts (optional)

□ Step 6: Share (Optional)
   □ Share application URL with others
   □ Update LinkedIn with deployment success
   □ Add screenshots to GitHub README
```

## Troubleshooting Decision Tree

```
                   Is deployment failing?
                          │
          ┌───────────────┴───────────────┐
          │                               │
         Yes                             No
          │                               │
          ▼                               ▼
   Check deployment logs          Application not loading?
   (Azure Portal or CLI)                  │
          │                      ┌────────┴────────┐
          │                      │                 │
          ▼                     Yes               No
   Error in build?              │                 │
   • Check Dockerfile           ▼                 ▼
   • Verify image exists    Check health      Working! 🎉
   • Check ACR access       endpoints         Monitor and
          │                 /health            optimize
          │                     │
          ▼                     ▼
   Fix and redeploy        Failing?
   ./deploy-azure.sh       │
                          Yes──► Check logs
                                 Review CORS
                                 Verify URLs
```

## Next Steps After Successful Deployment

1. **Bookmark URLs**
   - Client: https://signalr-client.*.azurecontainerapps.io
   - Server: https://signalr-server.*.azurecontainerapps.io
   - Health checks: /health on both

2. **Set Up Monitoring** (optional)
   - Enable Application Insights
   - Configure alert rules
   - Set cost budgets

3. **Enhance Application** (optional)
   - Add authentication
   - Connect to database
   - Implement more features

4. **Share Your Success**
   - Update GitHub README with live URLs
   - Share on LinkedIn
   - Demo to your team

---

**Ready to deploy?** Run: `./deploy-azure.sh`

**Need help?** Check: `AZURE-QUICKREF.md`

**Questions?** Review: `AZURE.md` and `AZURE-CONFIG.md`
```
